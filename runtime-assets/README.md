# Bardheim Runtime Assets

Bardheim no longer uses side-loaded runtime visual assets. The custom
OBJ, Unity bundle, and prefab-held visual direction was abandoned after manual
testing showed poor iteration value and a major FPS drop while holding the
custom item.

Keep this directory empty unless a future approved milestone reintroduces a
runtime asset path.
